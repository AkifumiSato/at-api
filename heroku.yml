build:
  docker:
    web:
      dockerfile: Dockerfile
      target: production
    release:
      dockerfile: Dockerfile
      target: build-stage
      command:
        - diesel setup
run:
  web: /api/my_app

build:
  docker:
    web:
      dockerfile: Dockerfile
      target: production
    release:
      dockerfile: Dockerfile
      target: build-stage
release:
  image: release
  command:
    - diesel setup
run:
  web: /api/my_app
